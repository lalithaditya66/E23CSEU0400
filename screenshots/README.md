# Screenshots Folder

Add the final proof images here with exact names:

- `stage6_priority_inbox_output.png`
- `vehicle_scheduling_output.png`

Tips:
- Keep the command used visible in the terminal capture.
- Ensure all required fields are visible in each screenshot.
